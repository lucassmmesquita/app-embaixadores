# ═══════════════════════════════════════════════════════════════
#  ⚠️  DEPRECATED — Use infra/compose/ instead
#
#  Development:  docker compose -f infra/compose/docker-compose.dev.yml up
#  Production:   docker compose -f infra/compose/docker-compose.prod.yml up -d
#
#  This file is kept only as a convenience alias for development.
#  It forwards to the dev compose file.
# ═══════════════════════════════════════════════════════════════

include:
  - infra/compose/docker-compose.dev.yml
