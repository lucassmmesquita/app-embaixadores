# ═══════════════════════════════════════════════════════════
#   Dev Dependencies (install with: pip install -r requirements-dev.txt)
# ═══════════════════════════════════════════════════════════

-r requirements.txt

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# Linting (Rust binary — no compilation needed)
ruff>=0.8.0
